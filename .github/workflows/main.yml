name: Build Android APK (Fast)

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # Builds only for modern 64-bit phones (arm64-v8a) with parallel compilation
      - name: Fast Build with Gradle (arm64-v8a only)
        run: |
          ./gradlew assembleAfatDebug \
            -Dorg.gradle.parallel=true \
            -Dorg.gradle.caching=true \
            -Dorg.gradle.jvmargs="-Xmx6g -XX:MaxMetaspaceSize=1024m" \
            -PtargetAbi=arm64-v8a

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Telegram-DeepVoice-APK
          path: "**/build/outputs/apk/**/*.apk"